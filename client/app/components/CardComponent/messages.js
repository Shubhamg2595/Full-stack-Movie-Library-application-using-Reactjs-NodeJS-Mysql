/*
 * CardComponent Messages
 *
 * This contains all the text for the CardComponent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CardComponent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CardComponent component!',
  },
});
