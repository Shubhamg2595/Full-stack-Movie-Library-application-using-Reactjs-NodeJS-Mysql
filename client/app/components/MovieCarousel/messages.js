/*
 * MovieCarousel Messages
 *
 * This contains all the text for the MovieCarousel component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MovieCarousel';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MovieCarousel component!',
  },
});
