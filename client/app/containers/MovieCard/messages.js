/*
 * MovieCard Messages
 *
 * This contains all the text for the MovieCard container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MovieCard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MovieCard container!',
  },
});
