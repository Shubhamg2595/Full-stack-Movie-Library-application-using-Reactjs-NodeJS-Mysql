/*
 * WatchList Messages
 *
 * This contains all the text for the WatchList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WatchList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WatchList container!',
  },
});
