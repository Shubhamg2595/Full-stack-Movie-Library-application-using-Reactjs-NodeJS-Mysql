/*
 * Search Messages
 *
 * This contains all the text for the Search container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Search';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Search container!',
  },
});
